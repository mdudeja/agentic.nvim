export enum AgentEventNames {
  created = 'agent.created',
  loaded = 'agent.loaded',
  spawned = 'agent.spawned',
  killed = 'agent.killed',
  // message = 'agent.message',
  // error = 'agent.error',
}
